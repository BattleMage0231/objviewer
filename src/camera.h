#pragma once

#include <glm/glm.hpp>

/*
class Camera {
    glm::vec3 position;
    glm::vec3 dir;
    glm::vec3 up;
    glm::vec3 right;
    glm::vec4 view;

    void computeVectors() const;

public:
    Camera(glm::vec3 position, glm::vec3 dir);
}
*/
