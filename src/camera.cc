#include "camera.h"

/*
Camera::Camera(glm::vec3 position) : position {std::move(position)}, dir {glm::normalize(dir)} {
    computeVectors();
}

void Camera::computeVectors() const {
    right = glm::normalize(glm::cross(glm::vec3(0.0f, 1.0f, 0.0f), dir));
    up = glm::cross(dir, right);
    //view = glm::lookAt(position, position + )
}
*/
